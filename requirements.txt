# 核心框架
torch>=2.1
transformers>=4.40
datasets>=2.14

# GPU 加速 (可选，无 GPU 时跳过；tokenizer Step 2/4 强依赖 cuDF/cuPy)
# 注意：RAPIDS wheel 只发布在 NVIDIA 索引上，且包名带 -cu12 后缀。
# 启用方式（不要直接 pip install 下面的裸包名，会解析到错误的旧 sdist）：
#   pip install -e ".[gpu]" --extra-index-url https://pypi.nvidia.com
# 等价于显式声明：
#   cudf-cu12>=24.10
#   cuml-cu12>=24.10
#   cupy-cuda12x>=13.0
# RAPIDS 24.x 覆盖 Python 3.9–3.12。

# 机器学习
xgboost>=2.0
scikit-learn>=1.3

# 数据处理
pandas>=2.1
numpy>=1.24
pyarrow>=14.0            # parquet 读写引擎

# 可视化
matplotlib>=3.7
seaborn>=0.13
plotly>=5.18

# 训练框架 (NeMo AutoModel, 可选)
# nemo-automodel>=0.1
# liger-kernel
# torchdata>=0.7            # NeMo 调用的 StatefulDataLoader

# 工具
pyyaml>=6.0
tqdm>=4.65
